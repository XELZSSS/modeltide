{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "rules": {
    "rules-of-hooks": "error",
    "exhaustive-deps": "error",
    "only-export-components": "off",
    "jsx-key": "error",
    "no-children-prop": "error",
    "no-direct-mutation-state": "error",
    "no-string-refs": "error",
    "no-this-in-sfc": "error",
    "no-render-return-value": "error",
    "jsx-no-duplicate-props": "error",
    "jsx-no-undef": "error",
    "void-dom-elements-no-children": "error",
    "no-unused-vars": "error"
  },
  "overrides": [
    {
      "files": ["src/client/**/*.ts", "src/client/**/*.tsx"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "patterns": [
              {
                "group": ["@/server", "@/server/*", "**/server/**", "@/worker", "@/worker/*", "**/worker/**"],
                "message": "Client code must not import server or worker code. Move the shared piece into src/shared."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/server/**/*.ts", "worker/**/*.ts"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "patterns": [
              {
                "group": ["@/client", "@/client/*", "**/client/**"],
                "message": "Server and worker code must not import the client bundle. Move the shared piece into src/shared."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/shared/**/*.ts"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "patterns": [
              {
                "group": ["@/client", "@/client/*", "**/client/**"],
                "message": "src/shared must stay platform-neutral. Move the client piece into src/client."
              },
              {
                "group": ["@/server", "@/server/*", "**/server/**", "@/worker", "@/worker/*", "**/worker/**"],
                "message": "src/shared must stay platform-neutral. Move the server piece into src/server."
              }
            ]
          }
        ]
      }
    }
  ]
}
