# Installs use the npmmirror registry; `npm run audit` intentionally overrides
# to https://registry.npmjs.org (audit metadata is authoritative there).
# Behavior is unchanged: installs stay locked via package-lock.json.
registry=https://registry.npmmirror.com/
engine-strict=true
