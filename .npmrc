# Do not pin a registry here: Workers Builds / CI must use the default
# https://registry.npmjs.org (keeps package-lock "resolved" URLs valid).
# For local speedup in CN, set the mirror in your own ~/.npmrc instead:
#   registry=https://registry.npmmirror.com
engine-strict=true
